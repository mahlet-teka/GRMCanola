##This is simple R code

A code for making Genomic relationship matrix(GRM) from a
genotypic data that is saved in .CSV and the frequency table.
 
 The for loop with write out the GRM in the format usable by ASReml, which is a three column data frame.